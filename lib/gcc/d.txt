gcc main.c -o file.exe -I. -L. -lwindivert
if (!WinDivertSend(handle, packet, packetLen, &sendLen, &addr)) {

    gcc main.c -o file.exe -I. -L. -lwindivert
 

       {
            if (!WinDivertSend(handle, packet, packetLen, &addr, &sendLen))
            {
                printf("Failed to send packet (%d)\n", GetLastError());
            }

gcc main.c -o pingspoofer -lws2_32
